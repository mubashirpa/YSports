<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.YSports" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/onPrimaryContainer</item>
        <item name="colorPrimaryInverse">@color/inversePrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/onSecondary</item>
        <item name="colorSecondaryContainer">@color/secondaryContainer</item>
        <item name="colorOnSecondaryContainer">@color/onSecondaryContainer</item>
        <!-- Tertiary brand color. -->
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorOnTertiary">@color/onTertiary</item>
        <item name="colorTertiaryContainer">@color/tertiaryContainer</item>
        <item name="colorOnTertiaryContainer">@color/onTertiaryContainer</item>

        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/onError</item>
        <item name="colorErrorContainer">@color/errorContainer</item>
        <item name="colorOnErrorContainer">@color/onErrorContainer</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="colorSurfaceVariant">@color/surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/onSurfaceVariant</item>
        <item name="colorSurfaceInverse">@color/inverseSurface</item>
        <item name="colorOnSurfaceInverse">@color/inverseOnSurface</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorOutline">@color/outline</item>
        <item name="elevationOverlayColor">@color/surfaceTint</item>

        <!-- Customize your theme here. -->

        <!-- Transparent system bars for edge-to-edge. -->
        <!-- Status bar. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <!-- Navigation bar. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">?attr/isLightTheme
        </item>
        <!-- For overflow menu -->
        <item name="popupMenuBackground">@drawable/bg_popupmenu</item>
        <!-- For popup menu -->
        <item name="popupMenuStyle">@style/Widget.YSports.PopupMenu</item>
        <item name="android:popupMenuStyle">@style/Widget.YSports.PopupMenu</item>
        <!-- For alert dialog -->
        <item name="alertDialogTheme">@style/ThemeOverlay.YSports.AlertDialog
        </item> <!-- For preference dialog -->
        <item name="android:alertDialogTheme">@style/ThemeOverlay.YSports.AlertDialog</item>
        <!-- For ModalBottomSheet -->
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.YSports.BottomSheetDialog</item>
    </style>

    <style name="Theme.YSports.PlayerActivity" parent="Theme.YSports">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges
        </item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.YSports.YouTubePlayerActivity" parent="Theme.YSports">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.YSports.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_icon_splash</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.YSports</item>
    </style>

    <!-- OSS Licenses plugin activity theme -->
    <style name="Theme.YSports.OssLicensesActivity" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/onPrimaryContainer</item>

        <item name="android:statusBarColor">@color/transparent_50</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">?attr/isLightTheme
        </item>

        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/onBackground</item>
    </style>

    <!-- Browser Activity Toolbar Theme -->
    <style name="Widget.YSports.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="titleTextAppearance">@style/TextAppearance.YSports.TitleLarge</item>
        <item name="subtitleTextAppearance">@style/TextAppearance.YSports.TitleMedium</item>
    </style>

    <style name="ThemeOverlay.YSports.BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.YSports.BottomSheet.Modal</item>
    </style>

    <style name="ThemeOverlay.YSports.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="dialogCornerRadius">@dimen/corner_radius_extra_large</item>
        <item name="android:colorBackground">@color/alertDialogBackground</item>
    </style>

    <style name="ThemeOverlay.YSports.TrackSelectionDialog" parent="ThemeOverlay.YSports.AlertDialog">
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Widget.YSports.PopupMenu" parent="Widget.Material3.PopupMenu">
        <item name="android:popupBackground">@drawable/bg_popupmenu</item>
    </style>

    <!-- A theme overlay for dialogs with a center aligned title text, icon and buttons that span the width of the dialog. -->
    <style name="ThemeOverlay.YSports.MaterialAlertDialog.Centered.FullWidthButtons" parent="ThemeOverlay.Material3.MaterialAlertDialog.Centered">
        <!-- Mark spacer as gone when showing full width buttons -->
        <item name="materialAlertDialogButtonSpacerVisibility">2</item>
        <item name="buttonBarPositiveButtonStyle">
            @style/Widget.Catalog.Button.TextButton.Dialog.FullWidth
        </item>
        <item name="buttonBarNegativeButtonStyle">
            @style/Widget.Catalog.Button.TextButton.Dialog.FullWidth
        </item>
        <item name="buttonBarNeutralButtonStyle">
            @style/Widget.Catalog.Button.TextButton.Dialog.FullWidth
        </item>
    </style>
</resources>